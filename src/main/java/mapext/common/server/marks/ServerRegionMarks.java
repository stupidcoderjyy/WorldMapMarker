package mapext.common.server.marks;

import mapext.common.marks.RegionMarkData;
import mapext.common.marks.RegionMarks;

public class ServerRegionMarks extends RegionMarks {
    
    public ServerRegionMarks(RegionMarkData markData) {
        super(markData);
    }
}
