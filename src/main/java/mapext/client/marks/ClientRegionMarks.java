package mapext.client.marks;

import mapext.common.marks.RegionMarkData;
import mapext.common.marks.RegionMarks;

public class ClientRegionMarks extends RegionMarks {
    public ClientRegionMarks(RegionMarkData markData) {
        super(markData);
    }
}
